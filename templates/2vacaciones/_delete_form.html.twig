<form method="post" action="{{ path('app_vacaciones_delete', {'idvacaciones': vacacione.idvacaciones}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ vacacione.idvacaciones) }}">
    <button class="btn">Delete</button>
</form>
