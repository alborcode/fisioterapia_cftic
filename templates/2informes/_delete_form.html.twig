<form method="post" action="{{ path('app_informes_delete', {'idinforme': informe.idinforme}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ informe.idinforme) }}">
    <button class="btn">Delete</button>
</form>
