<form method="post" action="{{ path('app_especialidades_delete', {'especialidad': especialidade.especialidad}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ especialidade.especialidad) }}">
    <button class="btn">Delete</button>
</form>
