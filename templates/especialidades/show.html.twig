{% extends 'base.html.twig' %}

{% block title %}Especialidades{% endblock %}

{% block body %}
    <h1>Especialidades</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Idespecialidad</th>
                <td>{{ especialidade.idespecialidad }}</td>
            </tr>
            <tr>
                <th>Especialidad</th>
                <td>{{ especialidade.especialidad }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_especialidades_index') }}">back to list</a>

    <a href="{{ path('app_especialidades_edit', {'especialidad': especialidade.especialidad}) }}">edit</a>

    {{ include('especialidades/_delete_form.html.twig') }}
{% endblock %}
