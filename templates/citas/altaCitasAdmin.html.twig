{% extends 'base.html.twig' %}

{% block title %}Añadir Citas{% endblock %}

{% block body %}

    <section class="py-5 mt-5">
        <div class="container py-5">
            <div class="row d-flex justify-content-center">
                <div class="col-md-6 col-xl-12">
                    <div class="card">
                        <div class="card-body text-center d-flex flex-column align-items-center">
                            <div class="row mb-4 mb-lg-5">
                                <p class="fw-bold text-success mb-2">Intranet - Menu Administrativo</p>
                                <h3 class="fw-bold">Añadir Citas</h3>
                            </div>
                            <div class="row mb-4 mb-lg-2">
                                {# Incluimos Boton Volver #}
                                {% include 'menuVolver.html.twig' %}
                            </div>
                            <div class="form-group col-sm-8">
                                {# Formulario Para Añadir/Modificar Turnos #}
                                <form method="POST" action="{{ path('altaVacacionesAdmin', { idfacultativo: datosFacultativo.idfacultativo }) }}"> 
                                    <div class="row mb-2 mb-lg-2">
                                        <div class="col-sm-4">  
                                            <select name="comboEspecialidad" class="form-control" id="comboEspecialidad" readonly="readonly" disabled="disabled">
                                                {% for Especialidad in datosEspecialidades %}
                                                    {% if Especialidad == datosFacultativo.especialidad %}
                                                        {# Se le pone atributo selected para que se quede seleccionado #}
                                                        <option value="{{ Especialidad.idespecialidad }}" selected> {{ Especialidad.especialidad }}</option>
                                                    {% else %}                                        
                                                        <option value="{{ Especialidad.idespecialidad }}"> {{ Especialidad.especialidad }}</option>
                                                    {% endif %}
                                                {% endfor %}
                                            </select>
                                        </div>
                                        <div class="col-sm-8">
                                            <input type="text" name="txtNombre" value="{{datosFacultativo.nombre}} {{datosFacultativo.apellido1}} {{datosFacultativo.apellido2}}" readonly="readonly" disabled="disabled" class="form-control" id="txtNombre">
                                        </div>
                                    </div>  
                                    <div class="row mb-2 mb-lg-2">
                                        <div class="col-sm-9">
                                            Paciente
                                            <input type="text" name="txtNombre" value="{{datosPaciente.nombre}} {{datosPaciente.apellido1}} {{datosPaciente.apellido2}}" class="form-control" id="txtNombre">
                                        </div>
                                        <div class="col-sm-3">
                                            Teléfono
                                            <input type="tel" name="txtTelefono" value="{{datosPaciente.telefono}}" class="form-control" id="txtTelefono" pattern="(\+34|0034|34)?[ -]*(6|7|8|9)[ -]*([0-9][ -]*){8}">
                                        </div>
                                    </div>  
 
                                    <div class="row mb-2 mb-lg-2">
                                         <div class="col-sm-6">
                                            <input type="text" class="form-control mb-lg-2 py-2" id="txFecha" name="txFecha" placeholder="AAAA/MM/DD" autocomplete="false" readonly="readonly">
                                        </div>
                                        {# <div class="col-sm-6">
                                            Seleccionar Rango Fechas
                                            <div id="select_rangedate">
                                                <input type="hidden" name="select_rangedate_value" class="date_output" value="{{ fechadia }}">
                                        </div> #} 
                                    </div> 
                                    <div class="row mb-1 mb-lg-1 py-1">
                                        <div class="col-sm-4 col-xl-4">
                                            <button type="submit" class="btn btn-primary" name="operacion" value="insertar">
                                                <i class="fa fa-chevron-circle-down" aria-hidden="true"></i> Añadir
                                            </button>
                                        </div>
                                    </div>    

                                </form>

                                <HR>
                                {% if mensaje is defined %}
                                    {% if mensaje is not null %}
                                        <div class="alert alert-success d-flex align-items-center alert-dismissible fade show" role="alert">
                                            {{mensaje}}
                                            <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
                                        </div>
                                    {% endif %}
                                {% endif %}
                                {% if mensajewarning is defined %}        
                                    {% if mensajewarning is not null %}
                                        <div class="alert alert-warning d-flex align-items-center alert-dismissible fade show" role="alert">
                                            {{mensajewarning}}
                                            <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
                                        </div>
                                    {% endif %}
                                {% endif %}     

                                {# {{  dump (festivosregionales) }} #}
                                {# Paso Array de Festivos Regionales en div para Jquery #}
                                {# <div id="your-div" data-yourArray="{{ festivosregionales|json_encode| raw }}">  #}
                                {# Muestro Tabla con Vacaciones existentes ya solicitadas #}
                                <div class="row mb-2 mb-lg-2">
                                    <table class="table">
                                        <thead>
                                            <tr>
                                                <th>Citas Solicitadas</th>
                                            </tr>
                                        </thead>
                                        <tbody>
                                            <div id="swup">
                                                {# Si esta definida la variable muestra los datos de las citas disponibles del facultativo #}
                                                {% if datosVacaciones is defined %}
                                                    {% for citas in datosCitas %}    
                                                        <tr>
                                                            <td scope="row">{{ citas.fecha |date('d-m-Y')}}</td>
                                                            <td>
                                                                <a class="btn btn-info" href="{{ path('bajaCitaAdmin', {idfacultativo: Facultativo.idfacultativo }) }}">Pedir Cita</a>
                                                            </td>
                                                        </tr>
                                                    {% endfor %}
                                                {% endif %}
                                            </div>
                                        </tbody>
                                    </table> 
                                </div>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </section>

    <script>
        const fechainicial = "{{ fechaini }}";
        console.log(fechainicial)
        const fechafinal = "{{ fechafin }}";
        console.log(fechafinal)
        const fechaactual = "{{ fechadia }}";
        console.log(fechaactual)
        const fechasfestivos = JSON.parse('{{ fechasnodisponibles | json_encode | raw }}');
        console.log(fechasfestivos)
        
        // Extraer de Array las fechas y montar fechas entre comillas separadas por comas
        // var concatena = "[";
        // $.each(fechasfestivos, (index, value) =>
        // {
        //     concatena = concatena+'"'+value+'"'+',';
        //     console.log(concatena)
        // });
        // // encuentra la ultima ocurencia de coma
        // var n = concatena.lastIndexOf(",");
        // //saca la cadena de texto hasta la ultima ocurencia de coma
        // var dates = concatena.substring(n, -1)
        // console.log(dates)
        // dates = dates+']';
        // console.log(dates)

        //var dates = ["2022-09-20", "2022-09-21"];
        // Funcion que desactiva Festivos
        //function DisableDates(date) {
        //    var string = jQuery.datepicker.formatDate('yy-mm-dd', date);
        //    return [fechasfestivos.indexOf(string) == -1];
        //}

        // Funcion que desactiva Festivos y fines de semana
        function noWeekendsOrHolidays(date) {
            var noWeekend = $.datepicker.noWeekends(date);
            if (noWeekend[0]) {
                var string = jQuery.datepicker.formatDate('yy-mm-dd', date);
                return [fechasfestivos.indexOf(string) == -1];
            } else {
                return noWeekend;
            }
        }

        $(function() {
            $.datepicker.setDefaults( $.datepicker.regional[ "es" ] );
            $("#txFecha").datepicker({
                firstDay: 1,
                //beforeShowDay: $.datepicker.noWeekends,
                //beforeShowDay: DisableDates,
                beforeShowDay: noWeekendsOrHolidays,
                autoSize: true,
                changeYear: true,
                changeMonth:true,
                dateFormat: 'yy-mm-dd',
                minDate: new Date(fechainicial),
                maxDate: new Date(fechafinal),
                showOn: "button",
                //showOn: "both"
                buttonText: "Calendario  ",
                showAnim: "fold",
                //showButtonPanel: true,
                //buttonImage: "/images/datepicker.gif"
                //buttonImageOnly: true
            }).next('button').append('<i class="fa fa-calendar"></i>');
            
            //$.datepicker.parseDate('dd-mm-yy', '22/04/2010');
            // $( "#txFecha" ).datepicker("show");
        });    
    </script>
    
{% endblock %}