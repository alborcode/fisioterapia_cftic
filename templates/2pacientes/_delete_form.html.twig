<form method="post" action="{{ path('app_pacientes_delete', {'idpaciente': paciente.idpaciente}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ paciente.idpaciente) }}">
    <button class="btn">Delete</button>
</form>
