<form method="post" action="{{ path('app_facultativos_delete', {'idfacultativo': facultativo.idfacultativo}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ facultativo.idfacultativo) }}">
    <button class="btn">Delete</button>
</form>
