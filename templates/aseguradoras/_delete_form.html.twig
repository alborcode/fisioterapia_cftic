<form method="post" action="{{ path('app_aseguradoras_delete', {'idaseguradora': aseguradora.idaseguradora}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ aseguradora.idaseguradora) }}">
    <button class="btn">Delete</button>
</form>
